namespace CraftSharp
{
    public enum RenderType {
        // Basic render types
        SOLID,
        CUTOUT,
        CUTOUT_MIPPED,
        TRANSLUCENT,
        // More render types defined for finegrained rendering
        UNLIT,
        WATER,
        FOLIAGE,
        PLANTS,
        TALL_PLANTS,
    }
}